export enum ExitCode {
    Success = 0,
    Failure = 1,
    Cancelled,
    Timeout,
    Unknown,
    Unsupported,
    Unauthorized,
    NotFound,
    Invalid,
    MissingArgument,
    InvalidArgument,
}
